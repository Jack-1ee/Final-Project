# 1. 狠心拒絕所有檔案 (預設全部忽略)
*

# 2. 但不要忽略資料夾 (這樣才能掃描到資料夾裡面的檔案)
!*/

# ============================
# 3. VIP 白名單 (只有這些能通過)
# ============================

# 當然要保留這份設定檔自己
!.gitignore

# 保留程式碼
!*.c
!*.h

# 保留指定的圖片
!Kimberly.bmp

# 保留 Workflow (GitHub Actions 標準資料夾)
!.github/
!.github/**/*

# (如果你說的 workflow 是指某個特定檔案，請自己加一行，例如: !workflow.txt)

# 雖然我們保留 *.c，但特別排除這個暫存檔
tempCodeRunnerFile.c